[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -q --tb=short -p no:cacheprovider
# 屏蔽 chromadb / torch / passlib 等第三方库的噪音警告，但不屏蔽本仓库代码的警告
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
